allOf:
  - $ref: "core/openapi/schemas/descriptionType.yaml"
  - type: object
    required:
      - version
    properties:
      version:
        type: string
      jobControlOptions:
        type: array
        items:
          $ref: "core/openapi/schemas/jobControlOptions.yaml"
      outputTransmission:
        type: array
        items:
          $ref: "core/openapi/schemas/transmissionMode.yaml"
      links:
        type: array
        items:
          $ref: "core/openapi/schemas/link.yaml"
